# phys476-dnn-cnn

This project implements both a fully connected neural network (DNN) and a convolutional neural network (CNN) for MNIST digit classification.

See [`REPORT.md`](./REPORT.md) for the written report.